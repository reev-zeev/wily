# Docker Compose for local Supabase development
version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: wasla_postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Optional: Add pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: wasla_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@wasla.local
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
